# Angular-Calendar-Scheduler-Master
Angular calendar scheduler. In this project i used angular calendar scheduler to show the events in calendar view where users can see the events in a calendar format.
